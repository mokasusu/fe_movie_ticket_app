  import 'package:flutter/material.dart';

  class AppColors {

    // ===== BACKGROUND =====
    static const Color bgPrimary = Color(0xFF0D0D0F);  // Nền chính toàn app (Scaffold, nền trang)
    static const Color bgSecondary = Color(0xFF16161A);  // Nền phụ – card phim, section, danh sách
    static const Color bgElevated = Color(0xFF1F1F25);  // Nền nổi – popup, modal, seat map, bottom sheet

    // ===== ACCENT / ĐIỂM NHẤN =====
    static const Color gold = Color(0xFFE4B15E);  // Vàng điện ảnh – nút Đặt vé, giá tiền, VIP, điểm nhấn quan trọng
    static const Color red = Color(0xFFC62828);  // Đỏ thảm đỏ
    static const Color neonBlue = Color(0xFF4FC3F7);  // Xanh neon – link, liên kết, trạng thái tương tác

    // ===== TEXT =====
    static const Color textPrimary = Color(0xFFFFFFFF);  // Chữ chính
    static const Color textSecondary = Color(0xFFB0B0B5);  // Chữ phụ
    static const Color textMuted = Color(0xFF7A7A80);  // Chữ mờ
    static const Color disabled = Color(0xFF4A4A4F);  // Trạng thái disabled
  }
