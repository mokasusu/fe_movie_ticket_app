import 'package:flutter/material.dart';

class Promotion {
  final String title;
  final Color color;
  final String imagePath;

  const Promotion({
    required this.title,
    required this.color,
    required this.imagePath,
  });
}
